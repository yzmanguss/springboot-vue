<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yunyun.financemanager.contract.mapper.PhaseMapper">
    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        insert into phase(phase_index, amount, start_time, contract_id)
        VALUES
        <foreach collection="phases" separator=", " item="Phase">
            (#{Phase.phaseIndex}, #{Phase.amount}, #{Phase.startTime}, #{phases.contract_id})
        </foreach>
    </insert>

    <delete id="deleteByContractId">
       delete
       from phase
       where contract_id = #{contractId}
    </delete>

<!--    <select id="phaseStatistics"  resultType="java.lang.Integer">-->
<!--    	select sum(amount) as phaseAmountMonth-->
<!--    	from phase-->
<!--    	where date_format(start_time,'%Y-%m') = date_format(now(),'%Y-%m')-->
<!--	</select>-->
</mapper>